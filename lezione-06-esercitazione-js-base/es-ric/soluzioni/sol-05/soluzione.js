// Soluzione ES-05 — Funzione freccia
// Consegna: scrivi una funzione freccia "moltiplica" che riceve due numeri
// e restituisce il prodotto. Testala con 3 coppie di numeri diversi.

const moltiplica = (a, b) => {
  return a * b;
};

console.log(moltiplica(2, 3));
console.log(moltiplica(5, 4));
console.log(moltiplica(10, 0.5));
